services:
  aiclient:
    image: aiclient-proxy:latest
    container_name: aiclient
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
      - ./configs:/configs
    network_mode: host