services:
  aiclient:
    image: justlikemaki/aiclient-2-api:latest
    container_name: aiclient
    restart: always
    network_mode: host
    environment:
      - NODE_ENV=production
      - GO_BACKEND_URL=http://localhost:35001
      - PYTHON_BACKEND_URL=http://localhost:35000
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
      - ./plugins:/app/src/plugins
