services:
  aiclient:
    image: justlikemaki/aiclient-2-api:latest
    container_name: aiclient
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GO_BACKEND_URL=http://192.168.7.103:35001
      - PYTHON_BACKEND_URL=http://192.168.7.103:35000
    networks:
      - default
      - go-vllm-api_default
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
      - ./plugins:/app/src/plugins

networks:
  default:
    driver: bridge
  go-vllm-api_default:
    external: true
